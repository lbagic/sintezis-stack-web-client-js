syntax = "proto3";

package coordinates.proto;

option go_package = "api-service-go/gen;gen";

// Coordinates contains latitude and longitude information
message Coordinates {
    float lat = 1;
    float lng = 2;
}