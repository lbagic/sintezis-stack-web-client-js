syntax = "proto3";

package elude.proto;

option go_package = "./;gen";

// Permission message is returned in GetPermissions rpc and as part of role message. Contains info on permissions inside of app.
message Permission {
    int32 id = 1; // Permission id.
    string name = 2; // Permission name.
}