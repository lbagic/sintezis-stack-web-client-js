syntax = "proto3";

package elude.proto;

option go_package = "./;gen";

// Coordinates are simple GPC coordinates (latitude and longitude)
message Coordinates {
    string latitude = 1; // Latitude coordinate
    string longitude = 2; // Longitude coordinate
}
